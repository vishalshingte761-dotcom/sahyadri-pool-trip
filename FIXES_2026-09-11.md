# Critical fixes — 2026-09-11

- Driver registration: declared the vehicle catalog before rendering, so the driver form no longer crashes while the catalog is loading.
- Driver route: preserved `/driver-register` and `#driver-register`.
- Nature search: searching now searches all 2,133 destination records, while the tabs remain section-scoped when no search term is entered.
- Jyotirlinga photos: exact Wikipedia title lookup with REST fallback; cache keys bumped to invalidate old repeated-photo results.
- Static JS/CSS cache-busted in index.html so browsers load this fix instead of stale app.js.
- Existing responsive CSS and other project flows were not intentionally changed.
